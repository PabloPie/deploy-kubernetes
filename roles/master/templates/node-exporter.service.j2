[Unit]
Description=node exporter service

[Service]
Restart=always
ExecStart=/usr/local/sbin/node_exporter --web.listen-address {{ hostvars[inventory_hostname]['ansible_default_ipv4']['address'] }}:9100
ExecStop=pkill node_exporter 

[Install]
WantedBy=local.target
